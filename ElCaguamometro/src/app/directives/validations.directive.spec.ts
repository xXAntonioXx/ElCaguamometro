import { ValidationsDirective } from './validations.directive';

describe('ValidationsDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationsDirective();
    expect(directive).toBeTruthy();
  });
});
